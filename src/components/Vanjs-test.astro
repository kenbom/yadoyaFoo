---

---

<script>
  import van from 'vanjs-core';
  const app = document.querySelector<HTMLDivElement>('#vanjs')!;

  const { button } = van.tags;
  const Button = () =>
    button(
      {
        style: () => `font-size:20px; color:green`,
        onclick: () => alert('Hello from 🍦VanJS'),
      },
      'Menu'
    );
  // van.add(document.body, Button());
  // van.add(
  //   document.body,
  //   button({ onclick: () => alert('Hello from 🍦VanJS') }, 'Hello')
  // );
  van.add(app, Button());
</script>
